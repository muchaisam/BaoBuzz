<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BaoBuzz" parent="Theme.Material3.DayNight.NoActionBar">


        <!--status bar color-->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="android:fontFamily">@font/ga_regular</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="SplashTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_background</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item> <!-- 1 second animation -->
        <item name="android:splashScreenTheme">@style/Theme.Material3.Light.NoActionBar</item>
    </style>

    <style name="CustomProgressDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:statusBarColor">@color/black</item>
    </style>
</resources>